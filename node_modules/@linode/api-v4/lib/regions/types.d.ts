import { COUNTRY_CODE_TO_CONTINENT_CODE } from './constants';
export type Capabilities = 'Bare Metal' | 'Block Storage' | 'Block Storage Migrations' | 'Cloud Firewall' | 'GPU Linodes' | 'Kubernetes' | 'Linodes' | 'Managed Databases' | 'Metadata' | 'NodeBalancers' | 'Object Storage' | 'Placement Group' | 'Premium Plans' | 'Vlans' | 'VPCs';
export interface DNSResolvers {
    ipv4: string;
    ipv6: string;
}
export type RegionStatus = 'ok' | 'outage';
export type RegionSite = 'core' | 'edge';
export interface Region {
    id: string;
    label: string;
    country: Country;
    capabilities: Capabilities[];
    placement_group_limits: {
        maximum_pgs_per_customer: number | null;
        maximum_linodes_per_pg: number;
    };
    status: RegionStatus;
    resolvers: DNSResolvers;
    site_type: RegionSite;
}
export interface RegionAvailability {
    available: boolean;
    plan: string;
    region: string;
}
type ContinentCode = keyof typeof COUNTRY_CODE_TO_CONTINENT_CODE;
export type Country = Lowercase<ContinentCode>;
export {};
//# sourceMappingURL=types.d.ts.map